attempt at making the frame container animate to larger failed
it looks like the scroll component makes a copy of that and renders with that, but the content updates...
its somehow holding the scroll size

